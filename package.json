{
  "name": "jacobs-clone",
  "version": "1.0.0",
  "description": "Jacob's Clone — a Slack app that lets you set a personal auto-responder. When someone messages a channel the bot is in, it waits your chosen delay, then replies as you.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "@slack/bolt": "^4.2.0",
    "@slack/web-api": "^7.8.0",
    "dotenv": "^16.4.5"
  }
}
